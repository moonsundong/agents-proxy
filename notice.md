配置步骤
1. 先在网关里备好模型和策略（浏览器开 http://localhost:5188):

模型管理 → 新增本地模型：base_url=http://127.0.0.1:7070(llama-server)，类型 local，设为默认
新增线上模型（OpenAI 或其他兼容服务），填真实 API Key
路由策略 → 新增策略：选好决策模型 / 本地模型 / 线上模型，阈值 0.7
2. 在 CC Switch 里新增一个供应商（Provider):

Base URL: http://localhost:8300/v1
API Key: 随便填一个占位值（网关跑在本地回环，目前没有鉴权，不校验 key)
协议/格式选 OpenAI 兼容（Chat Completions);Codex 走自定义供应商时 wire_api 选 chat
保存后切换到该供应商，Codex 桌面版的请求就会全部流经网关：压缩 → 决策路由 → 本地/线上
我们网关 Settings 页里的"CC Switch 端点管理"只是记录这些端点配置（方便导入导出备份），不影响流量；真正接管是靠上面 CC Switch 应用里的切换。

⚠️ 关键问题：Codex 总会带模型名，会绕过智能路由
Codex 发请求时 model 字段永远有值（比如 gpt-5-codex)。而网关现在的逻辑是：请求带 model = 手动覆盖，跳过决策路由；名字匹配不上我们的模型时 → 直接退回默认模型，永远不经过决策模型评估。

也就是说按现状，你 CC Switch 切过来后所有请求都走默认模型，智能路由形同虚设。两个修法：

（推荐）改网关逻辑：请求的模型名匹配不上任何已配置模型时，不视为手动覆盖，继续走策略路由（匹配上才算手动指定）。这样 Codex 的流量天然进入决策路由，而你在 UI 里测试时仍可用模型名手动指定
在网关里把某个模型的"名称"改成 Codex 发的那个名字（如 gpt-5-codex)，让它精确匹配——但这样所有请求都固定走这一个模型，等于放弃路由
要我按方案 1 改吗？改动很小（decision_service.resolve_route 的分支顺序 + 两个测试断言），改完 CC Switch 接进来就是完整的"压缩 → 决策 → 路由"链路。